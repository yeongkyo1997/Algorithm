length = 0

sale = [10, 20, 30, 40]


def combi(depth):


    def subset(users, depth, flag):
        for i in range(1, 1 << length):
            for j in range(length):


def solution(users, emoticons):
    global length, number
    length = len(users)
